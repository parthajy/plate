pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{ts,tsx,js,jsx,json,md,yml,yaml}'
      run: npx prettier --check {staged_files}
    eslint:
      glob: '*.{ts,tsx,js,jsx}'
      run: npx eslint {staged_files}
